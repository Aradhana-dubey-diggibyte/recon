bundle:
  name: recon-bundle

sync:
  include:
    - notebooks/*.ipynb
    - resources/**.yml
    - config/*.csv

targets:
  dev:
    mode: development
    default: true
    workspace:                 
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
