bundle:
  name: recon-bundle
include:
  - resources/*.yml
sync:
  include:
    - notebooks/*.ipynb
    - config/*.csv

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
